# Stage 6 staging-host baseline evidence

Status: `NOT_RUN_HOST_NOT_CREATED`

Required post-provision evidence: hostname, image/snapshot ID, kernel,
`libseccomp2`, runc, containerd, Docker/Compose, firewall read-back, disk,
memory, SSH identities, Docker socket exposure, default seccomp state, and the
known `docker exec ... true` regression probe.

No baseline claim is made by configuration alone.
